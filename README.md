# nmc-gnirehtet-build

Public GitHub Actions workflows only.

- Triggers are strictly `workflow_dispatch`.
- Source code is cloned from private source repo over SSH deploy key.
- Artifacts use short retention and can be deleted after download.
